# Brayan Brush API — compatible con Python 3.7
fastapi>=0.68.0,<0.100.0
uvicorn[standard]>=0.15.0
python-multipart>=0.0.5
pydantic>=1.10.0,<2.0.0
python-dotenv>=0.19.0

# Gemini (asistente en backend). Requiere Python 3.9+.
# En Python 3.7 no está disponible; la API funciona igual, el chat devuelve 503.
# Si usas Python 3.9+: pip install google-generativeai
# google-generativeai>=0.8.0

# MySQL (persistencia)
sqlalchemy>=1.4.0,<2.0.0
pymysql>=1.0.2
cryptography>=3.4.0
